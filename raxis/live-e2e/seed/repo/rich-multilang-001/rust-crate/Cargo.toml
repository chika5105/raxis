[package]
name        = "rust-crate"
version     = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Rust workspace member for the rich-multilang-001 e2e fixture."

[lib]
path = "src/lib.rs"

[dependencies]
