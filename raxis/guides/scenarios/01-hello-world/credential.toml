# Scenario 01 credentials — placeholders only; do NOT commit secrets.
#
# Hello World uses Anthropic only. The genesis ceremony already wrote
# anthropic-prod.toml at $RAXIS_DATA_DIR/providers/anthropic-prod.toml;
# fill in the api_key there. Confirm the file mode is 0600.

# Path: $RAXIS_DATA_DIR/providers/anthropic-prod.toml
# api_key = "sk-ant-REPLACE_ME"
