# Scenario 25 — wall-clock ceiling.

[plan.initiative]
description = """
Wall clock limit (scenario 25)
"""
[workspace]
name = "Wall Clock Limit"
lane_id     = "wallclock-lane"

[[tasks]]
task_id            = "slow"
session_agent_type = "Executor"
clone_strategy     = "blobless"
path_allowlist     = ["src/"]
wall_clock_seconds = 5
description = """
  Add an exhaustive 5,000-word essay as a comment in src/main.rs
  explaining each line of the program. Be very thorough.
"""
