# Scenario 26 — abort mid-flight.

[workspace]
name        = "Abort mid-flight"
lane_id     = "abort-lane"

[[tasks]]
task_id            = "long-running"
session_agent_type = "Executor"
clone_strategy     = "blobless"
path_allowlist     = ["src/"]
wall_clock_seconds = 600
context = """
  Add a 5,000-line auto-generated module to src/main.rs, with each
  line incrementing a counter. Take your time.
"""
