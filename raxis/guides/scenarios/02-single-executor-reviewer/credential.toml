# Scenario 02 credentials.
#
# This scenario only exercises the LLM provider (Anthropic by
# default). Provider credentials live under
# $RAXIS_DATA_DIR/providers/<name>.toml at mode 0600 and are managed
# outside any plan-attached `credential.toml`. The placeholder below
# is here only to remind the operator that this scenario does NOT
# need any agent-bound credentials.

# (intentionally empty)
