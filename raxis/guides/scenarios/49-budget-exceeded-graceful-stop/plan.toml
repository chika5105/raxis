# Scenario 49 — under-budget initiative.

[plan.initiative]
description = """
Budget exceeded graceful stop (scenario 49)
"""
[workspace]
name        = "Tiny budget"
lane_id     = "tiny-budget-lane"

[budget]
max_tokens          = 500
max_wall_clock_secs = 60

[[tasks]]
task_id            = "verbose-task"
session_agent_type = "Executor"
clone_strategy     = "blobless"
path_allowlist     = ["docs/"]
description = "Write a 5,000-word essay on monad transformers in docs/essay.md."
